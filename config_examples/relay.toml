# Relay server mode — run mcpr as the tunnel relay
# Start with: mcpr --relay

# Port the relay server listens on
port = 8080

# Base domain for tunnel subdomains (e.g. <subdomain>.tunnel.mcpr.app)
relay_domain = "tunnel.mcpr.app"
